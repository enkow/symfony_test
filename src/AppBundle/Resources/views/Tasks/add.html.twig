{% extends 'AppBundle::page.html.twig' %}

{% block body %}
    <h2>{{ 'task.add.title'|trans }}</h2>
    <div>
        {{ form_errors(form) }}
    </div>
    {{ form(form, {'method': 'POST'}) }}
{% endblock %}
